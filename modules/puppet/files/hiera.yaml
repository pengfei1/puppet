:backends:
    - yaml
    - json
:hierarchy:
    - common
    - "%{fqdn}"
    - "%{domain}"
    - "%{puppetrole}"
:yaml:
    :datadir: "/etc/puppet/environments/%{environment}/hiera"
:json:
    :datadir: "/etc/puppet/environments/%{environment}/hiera"
