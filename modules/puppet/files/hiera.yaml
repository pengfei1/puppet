:backends:
    - yaml
    - json
:hierarchy:
    - common
    - "%{fqdn}"
    - "%{domain}"
    - "%{is_puppet_master}"
    - "%{is_puppet_agent}"
:yaml:
    :datadir: "/etc/puppet/environments/%{environment}/hiera"
:json:
    :datadir: "/etc/puppet/environments/%{environment}/hiera"
