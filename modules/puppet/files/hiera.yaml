---
:backends:
    - yaml
    - json
:hierarchy:
    - common
    - "%{fqdn}"
    - "%{domain}"
    - "%{is_puppetdb_server}"
    - "%{is_puppet_master}"
    - "%{is_puppet_agent}"
    - "%{is_hbase_thrift}"
:yaml:
    :datadir: "/etc/puppet/environments/%{environment}/hiera"
:json:
    :datadir: "/etc/puppet/environments/%{environment}/hiera"
