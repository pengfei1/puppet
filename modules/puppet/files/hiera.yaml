:backends:
    - yaml
    - json
:hierarchy:
    - common
    - "%{fqdn}"
    - "%{domain}"
    - "%{puppet-master}"
    - "%{puppet-agent}"
:yaml:
    :datadir: "/etc/puppet/environments/%{environment}/hiera"
:json:
    :datadir: "/etc/puppet/environments/%{environment}/hiera"
